class Solution:
    # @param n: an integer
    # @return an integer f(n)
    def fibonacci(self, n):
        # write your code here
        a=0
        b=1
        if n==1:
            return a
        if n==2:
            return b
        for i in range(2,n):
            a,b=b,a+b
        return b
            
